# Legacy Parity Matrix

This matrix is the machine-checkable parity ledger for LizzieYzy Next against the Java/Swing legacy baseline. It is seeded from the current Planner findings in the architecture, migration, development, and release docs, plus the legacy main feature areas that users depend on.

Status tokens are intentionally narrow:

- `complete`: repository evidence supports the named legacy behavior for the scoped row.
- `partial`: the main path exists, but fixtures, edge cases, UI automation, or migration breadth are incomplete.
- `missing`: no accepted Next implementation or acceptance evidence exists yet.
- `external-validation-needed`: repository evidence may exist, but live provider, sidecar, platform, signing, or environment proof is required before claiming parity.

Do not use this matrix to claim full Java/Swing parity. A row reaches parity only when its acceptance evidence is recorded and any external gate is passed.

## UI

| Legacy Capability | Current Status | Acceptance Evidence | External Gate | Notes |
| --- | --- | --- | --- | --- |
| Main board review view with move navigation and visual board state | `partial` | Browser or Tauri smoke shows SGF load, board render, next/previous navigation, captures, pass moves, comments, and stable coordinates against golden SGF fixtures. The scoped `legacy_layout_parity_smoke` gate validates board visibility in required layout screenshots/viewports from `docs/qa/legacy-layout-parity-smoke-macos.json`; `docs/qa/legacy-shortcut-layout-evidence-macos.json` augments that evidence with scoped layout counts and a stable browser screenshot artifact. | UI automation evidence for the primary desktop smoke flow. | React board and analysis surfaces exist, but complete layout/theme parity and shortcut breadth are not closed. Scoped layout evidence must not be read as pixel-perfect or full legacy UI parity. |
| LegacyShell main menu View/Engine/Tools/Help entries | `partial` | Repository-local smoke statically checks `LegacyShell.tsx` together with `legacyActionMatrix` for `View`, `Engine`, `Tools`, and `Help` entries that are identifiable by label or `data-testid` and are not disabled-only placeholders. The scoped `legacy_shell_menu_action_smoke` gate validates browser-rendered click evidence for View/Candidates, View/Ownership, View/Policy, Engine/Profiles, Engine/Assets, Tools/Providers, Tools/Preferences, and Help/Backend status, including LegacyShell active-target attributes and visible target selectors. The scoped `native_menu_shortcut_smoke` gate validates macOS OS-native menu surface, native-menu event bridge, keyboard shortcut surface, aligned action ids, input-editing safety, Rust/frontend event-name alignment, `native_menu_contract` registration, and exact File/Game/Analysis/View/Engine/Tools/Help groups. The scoped `tauri_webview_dom_click_smoke` gate records Tauri WebView DOM/click proof with at least four clicked controls and visible target assertions. The scoped `legacy_layout_parity_smoke` evidence validates toolbar/menu visibility and layout boundaries. `docs/qa/legacy-shortcut-layout-evidence-macos.json` records runner-compatible scoped browser/runtime action matrix, clicked-control, shortcut, visible-target, viewport, and screenshot evidence. | Full shortcut parity, full legacy menu parity, full layout parity, release-target desktop behavior, and native dialog behavior still need dedicated evidence before full main-menu parity is claimed. | Current alpha gate passes static menu-surface, scoped browser-rendered menu-action evidence, scoped macOS native menu/shortcut source-fact evidence, scoped Tauri WebView DOM/click evidence, scoped layout evidence, and scoped action/shortcut/layout browser runtime evidence. This is not full OS-native menu parity, native dialog proof, full shortcut/layout parity, release parity, OCR capture parity, installed-app parity, provider/readboard/OCR parity, or full LegacyShell parity. |
| Analysis panel, winrate chart, candidate moves, PV display, and problem markers | `partial` | Tauri smoke records one-position and full-game analysis updating candidate moves, PV, ownership/policy data where available, winrate chart, and review markers without stale state. The scoped `legacy_layout_parity_smoke` evidence validates analysis panel, winrate, candidates/PV, and cache/status visibility. | Controlled KataGo or mock-process UI integration coverage. | MVP paths are present; advanced review workflows and every legacy shortcut remain open. Scoped layout evidence is not full analysis UI parity or release proof. |
| ProviderPanel as provider/readboard control surface | `partial` | ProviderPanel exposes provider fetch, readboard probe, readboard sync controls, scoped controlled board image import controls, and scoped legacy helper status cards for SGF/payload, protocol snapshot, OCR/image, and external window/client capture paths. It shows structured success/unavailable/error or recoverable-unsupported states without UI-only shortcuts. | Live Fox/Yike/readboard smoke before live support is claimed. Arbitrary screenshot OCR, external window/client capture, real client capture, full readboard parity, and full legacy OCR parity remain external gates. | If a build only offers URL preview or payload import, fetch/probe/sync UI remains pending. The scoped readboard runtime evidence gate is limited to probe/protocol semantics, and the controlled image import/corpus evidence is scoped to controlled fixtures only; neither proves arbitrary OCR or real external client/window capture. |

## SGF And Editing

| Legacy Capability | Current Status | Acceptance Evidence | External Gate | Notes |
| --- | --- | --- | --- | --- |
| SGF parse, replay, and board-state reconstruction | `complete` | Rust SGF and Go-domain tests pass against golden fixtures for FF4, 19x19 play, variations, comments, setup stones, captures, pass, suicide, and simple ko behavior. | None. | Current evidence supports the scoped MVP compatibility set, not every historical SGF edge case. |
| SGF tree, variations, and comments panel | `partial` | Acceptance requires API/DTO tests for stable tree node ids, parent/child variation links, move indexes, selected-node lookup, comment text exposure/save, node property exposure/save, SGF annotation persistence, branch node position replay, append move/pass as child or sibling variation, delete selected non-root node/subtree behavior, sibling variation reorder behavior, repository-local edit-existing-move command/frontend surface evidence, and UI smoke showing tree navigation, variation selection, comments panel updates, comment/property/annotation save, branch position display, append move/pass, edit existing move/pass, delete selected non-root subtree, variation reorder, and SGF reopen round-trip behavior from golden SGF fixtures. Repository evidence now includes `desktop_sgf_editing_ux_smoke` for scoped source-surface/source-visible LegacyShell/toolbar/tree/annotation/selected-node/dirty-saved UX fields plus committed Tauri runtime-chain coverage for those edit flows; it records `runtimeDomObserved=false` and `screenshotObserved=false`. It also includes `desktop_ui_click_smoke` and `legacy_shell_menu_action_smoke` for scoped browser-rendered DOM/click/screenshot/menu-action evidence with stable screenshot paths and `nativeFileDialogCovered=false`, `native_menu_shortcut_smoke` for scoped macOS native menu/shortcut surface evidence, `tauri_window_runtime_smoke` for scoped Tauri runtime/window screenshot evidence, `tauri_webview_dom_click_smoke` for scoped Tauri WebView DOM/click and visible-target evidence, `legacy_layout_parity_smoke` as scoped layout evidence for SGF tree and annotation/comment/properties visibility, `native_desktop_sgf_workflow` for scoped manual-assisted native desktop SGF open/edit/save/reopen evidence, and `installed_app_sgf_workflow` for scoped packaged macOS app runtime automation of SGF load/reparse/tree navigation/comment/property/annotation/append/edit/reorder/delete/save/readback/reopen/final invariant with backend runtime proof. | Broader UI automation and native desktop release smoke must still prove full OS-native menu parity, release-target breadth, full layout/shortcut parity, Windows/Linux installed-app behavior, signing/notarization/release readiness, and exploratory user behavior on each release target before broader edit parity is claimed. | Supports comment save, node property update, scoped TR/SQ/CR/MA/SL/LB/AR/LN annotation persistence, branch position replay, append move/pass as a child or sibling variation, edit-existing-move repository-local wiring evidence, delete selected non-root node/subtree, sibling variation reorder, a scoped macOS local runtime evidence gate for two-launch save/reopen, scoped Desktop SGF Editing UX source-surface/runtime-chain evidence, scoped browser-rendered DOM/click/screenshot/menu-action proof, scoped macOS native menu/shortcut surface proof, scoped Tauri runtime/window screenshot proof, scoped Tauri WebView DOM/click proof, scoped layout evidence, scoped manual-assisted native desktop SGF workflow proof, and scoped packaged macOS app SGF workflow automation proof. This is not full automation, full OS-native menu parity, full shortcut/layout parity, signed/notarized release proof, Windows/Linux installed-app proof, full layout/UI parity, legacy import/capture helpers, OCR, external client capture, provider/readboard parity, or broader SGF editing parity. |
| Native SGF open and save workflow | `partial` | Repository-local evidence is complete for read-back refresh wiring: `native_sgf_save_readback_surface` passes, the backend exposes native `read_sgf_file`/`write_sgf_file`, save reads the file back after writing, and App save handling reparses, replays, refreshes the tree, and checks cache from the read-back SGF text. The macOS local runtime evidence gate can also pass when the committed JSON includes `secondLaunch`, `reopen`, and `afterReopen` fields proving save/reopen after a second launch. `desktop_sgf_editing_ux_smoke` records source-visible dirty/saved state and save/readback/reopen runtime-chain coverage with `collectionMethod=source_static_plus_tauri_runtime_chain`, `runtimeDomObserved=false`, `screenshotObserved=false`, and `nativeDialogClickCovered=false`. Browser click evidence records `nativeFileDialogCovered=false`; `tauri_window_runtime_smoke` adds scoped Tauri runtime/window screenshot evidence; `tauri_webview_dom_click_smoke` adds scoped Tauri WebView DOM/click evidence. The `native_desktop_sgf_workflow` evidence records scoped manual-assisted native desktop SGF open/edit/save/reopen coverage with native open/save dialog step records, sanitized paths, screenshots, persisted edit evidence, and board/tree reopen invariants. The `installed_app_sgf_workflow` evidence records scoped packaged macOS app SGF load/edit/save/readback/reopen automation with backend runtime proof and no tauri-dev fallback. `packaged_native_dialog_sgf` now records scoped unsigned packaged macOS `.app` native Open/Save dialog SGF workflow proof with dialog records, screenshots, SGF hashes, content-preserved/readback/reopen invariants, and stricter artifact validation. | Release-target breadth is still required on each release target, and broader proof still needs Windows/Linux installed app coverage, signing/notarization/release readiness, full layout/shortcut parity, and exploratory user behavior before full native workflow parity is claimed. | This supports repository-local save/read-back wiring, scoped macOS local runtime save/reopen evidence, scoped source/static dirty/saved UX state, scoped browser-rendered click evidence, scoped Tauri runtime/window screenshot evidence, scoped Tauri WebView DOM/click evidence, scoped manual-assisted native desktop workflow evidence, scoped packaged macOS app SGF workflow automation, and scoped packaged native-dialog evidence. Browser preview, scoped browser-rendered click evidence, the scoped UX report, Tauri window screenshot gate, WebView DOM/click gate, packaged macOS SGF workflow gate, and packaged native-dialog gate are not full automation, signed/notarized release proof, Windows/Linux installed-app proof, OCR/capture/provider/readboard proof, full layout/shortcut parity, full native dialog parity, or full legacy parity. |
| Editing, annotation, and legacy import/capture helpers | `partial` | Acceptance requires tests and smoke evidence for editing existing moves, persisted properties/comments, annotation persistence, variation reorder, and legacy capture/import helpers beyond the current SGF flows. Repository-local evidence now includes `sgf_annotation_surface` for TR/SQ/CR/MA/SL/LB/AR/LN add/update/remove UI wired through `update_sgf_node_properties`, runtime smoke semantics for annotation add/update/remove in the same comment/property/existing-move/reorder/delete/save-readback-reopen chain, and `legacy_import_capture_helper_surface` for scoped ProviderPanel helper visibility. | Legacy fixture comparison for migrated helper behavior plus native desktop save/reopen smoke. OCR and external client capture remain external gates. | Scoped annotation persistence is covered for SGF markup properties only. The helper surface covers user-visible status and boundaries, not real OCR/image readboard, real external client/window capture, native file dialog/manual release proof, or broader annotation workflow parity. |
| Legacy import/capture/OCR helpers | `partial` | Repository smoke verifies a scoped ProviderPanel helper surface: SGF/payload and protocol snapshot helper paths are visible, OCR/image and external window/client capture helpers display structured recoverable-unsupported state, and unsupported helpers explicitly do not import SGF or replace the board with guessed/stale/partial data. `readboard_image_import_smoke` separately validates controlled image path/base64 import MVP evidence with invalid/non-board rejection and snapshot fields. `readboard_image_ocr_corpus_smoke` validates a scoped controlled fixture manifest with repo-relative artifact hash/size checks, path/base64 equivalence, invalid/non-board/truncated rejection, board-size/stone-count coverage, hash invariants, and unsupported external capture contract. `readboard_external_capture_mvp` validates scoped runtime-backed local-image backend capture/decode proof only. `readboard_operator_capture` validates scoped operator-selected preview/confirm/import proof with no automatic backend board replacement and UI replacement only after confirmation. | Arbitrary screenshot OCR environment validation, target-client discovery, real client parity, full readboard parity, and legacy helper fixture comparison are required before claiming migrated helper parity. | This row is intentionally separate from scoped SGF annotation persistence. The controlled image import, corpus proof, local-image backend decode proof, and operator-selected preview/confirm/import proof remain scoped and keep full OCR/readboard/client parity false. Arbitrary OCR, target client/window discovery, full legacy OCR parity, full readboard parity, and full legacy helper migration remain pending. |
| Controlled readboard image import MVP | `partial` | `docs/qa/readboard-image-import-smoke-macos.json` records scoped controlled image path and image base64 import evidence, invalid-image rejection, non-board-image rejection, snapshot id, board size, stone count, player-to-play, and protocol regression verification. `docs/qa/readboard-image-ocr-corpus-smoke-macos.json` records scoped controlled corpus evidence for fixture manifest metadata, path/base64 equivalence, invalid/non-board/truncated rejection, board-size/stone-count coverage, hash invariants, and unsupported external capture contract. | Broader OCR/capture gates with arbitrary screenshots, external windows/clients, real client capture, operator safety checks, and legacy fixture comparison remain required before OCR parity or full readboard parity is claimed. | This is controlled image import and controlled corpus evidence only. It is not arbitrary screenshot OCR, external window/client capture, real client capture, full legacy OCR parity, or full readboard parity. |
| Real OCR and external capture implementation | `partial` | `docs/qa/readboard-external-capture-mvp-macos.json` now records scoped runtime-backed local-image backend capture/decode proof. `scripts/smoke_tauri_readboard_external_capture_mvp.py` runs Tauri phase `readboard-external-capture-mvp` and aggregates `readboardExternalCaptureMvp` only when the runtime report contains backend command `readboard_external_capture`, `runtimeObserved=true`, `backendCommandInvoked=true`, `rawBackendResult.status=captured`, `captureSource=local_image`, position/decode proof, sanitized artifact metadata, and no synthesized operator selection, UI confirmation, or board replacement. `docs/qa/readboard-operator-capture-macos.json` records scoped operator-selected capture UI preview/confirm/import proof with runtime observed backend capture, operator/user selection, captured raw result, artifact hash/size/path, decoded position, snapshot hash, top-level `userConfirmed=true`, preview-only-before-confirmation, no automatic backend board replacement (`rawBackendBoardReplacement=none`), and UI board replacement only after confirmation (`uiBoardReplacedAfterConfirmation=true`). | Dedicated OCR/capture runtime gates with arbitrary source screenshots/windows, target-client metadata/discovery, real client parity, Windows/Linux capture behavior, and fixture comparisons against legacy behavior remain required. | Static/committed fixture evidence is rejected for PASS. The accepted local-image evidence is scoped backend decode only, and the operator-selected capture evidence is scoped preview/confirm/import proof only; do not claim arbitrary screenshot OCR, target-client discovery, real client parity, Windows/Linux capture proof, release proof, full OCR parity, full readboard parity, or full legacy OCR/helper parity. |
| Arbitrary screenshot OCR and target-client parity | `missing` | No accepted Next evidence exists yet for arbitrary screenshot OCR, target-client discovery, named external client parity, or cross-platform capture behavior. | Dedicated runtime gates must record target-client metadata, arbitrary screenshot fixtures, real client selection/discovery, Windows/Linux capture evidence, and legacy fixture comparisons before this can advance. | This missing row is intentionally separate from the scoped external capture MVP; do not use the MVP evidence to claim full OCR, target-client discovery, real client parity, Windows/Linux capture, release parity, or full readboard parity. |

## KataGo

| Legacy Capability | Current Status | Acceptance Evidence | External Gate | Notes |
| --- | --- | --- | --- | --- |
| Engine profile setup and asset validation | `partial` | Tauri smoke records binary, model, config, working directory, visits, asset-check success/failure, persisted profile reload, and clear validation errors. | OS-specific desktop smoke with local or bundled assets. | Multiple engine profiles and asset checks are present; full legacy settings migration is separate. |
| One-shot KataGo analysis | `partial` | `Run KataGo` in Tauri receives normalized JSONL analysis frames for the selected move, updates UI state, and records stderr/error propagation for failure cases. The repository gate requires both macOS live KataGo CLI evidence from `docs/qa/katago-live-smoke-macos.json` and Tauri runtime evidence from `docs/qa/katago-tauri-runtime-smoke-macos.json`, produced with a real binary/model/config. | Controlled KataGo fixture or real engine smoke with version/model details; desktop UI review-state proof still needed before claiming user-facing analysis parity. | Repository DTO/protocol paths exist, and the live evidence gate covers CLI version/JSONL plus Tauri analyze-once runtime invocation. Broader UI state update, failure handling, and every legacy analysis workflow remain incomplete. |
| Full-game analysis, progress, cancellation, and cache interaction | `partial` | Full-game batch analysis emits progress, supports cancellation, saves cache records, reloads cache hits, and does not report canceled work as success. The macOS live KataGo evidence gate covers CLI batch analysis and Tauri runtime analyze-game/start-cancel checks. The scoped `katago_review_workflow_ux_smoke` gate validates `docs/qa/katago-review-workflow-ux-smoke-macos.json` plus frontend source facts for visible progress/current/total/job id/session, cancel/restart, cache-hit restore source, engine failure messaging, and stale-analysis guard. The `katago_live_desktop_workflow_smoke` gate validates recorded `docs/qa/katago-live-desktop-workflow-smoke-macos.json` evidence for live KataGo desktop runtime progress, cancel/restart, completion, cache save, cache-hit restore with frame/candidate/winrate evidence, stale-cache prevention, engine failure observation, no browser fallback, and false boundaries for full legacy analysis, provider/readboard, release, and arbitrary OCR parity. The `installed_app_katago_live_workflow` gate validates recorded packaged macOS `.app` evidence from `docs/qa/installed-app-katago-live-workflow-macos.json` with real KataGo binary/model/config metadata, progress, cancel, restart, completion, cache save/hit, stale-cache guard, engine failure, screenshot metadata, and no browser/dev-server fallback. | Full review workflow or full legacy analysis parity still requires broader release-target and parity evidence. | MVP batch/cancel/cache paths exist. The review workflow UX resilience evidence is source-static plus stubbed UI-flow only with `liveKataGoObserved=false`; the live desktop and installed-app live KataGo gates are scoped live evidence only. These do not prove full KataGo parity, bundled large-model parity, signed release parity, provider/readboard parity, release parity, arbitrary OCR parity, or Windows/Linux behavior. |

## Provider And readboard

| Legacy Capability | Current Status | Acceptance Evidence | External Gate | Notes |
| --- | --- | --- | --- | --- |
| Yike provider fetch through `provider_fetch_yike` | `external-validation-needed` | Repository evidence requires offline contract tests and runtime path checks returning normalized DTOs or typed `ProviderError` values without UI-only shortcuts. The repository smoke gate can validate scoped macOS controlled-network Tauri evidence from `docs/qa/provider-live-smoke-macos.json` for `yike_controlled_fetch`, typed failure modes, controlled request observation, and explicit offline/external-service scope limits. | Fetch a real Yike resource and record account/session type, network conditions, result count, latency, and login/session-expiry behavior before claiming live Yike parity. | Controlled-network evidence is stronger than offline parser evidence, but it is not real Yike service parity, account login proof, anti-bot stability proof, or service schema drift proof. |
| Fox provider fetch through `provider_fetch_fox` | `external-validation-needed` | Repository evidence requires contract tests for `chessid`, `uid`, and `user_name`, plus runtime path checks for structured success/error results. The repository smoke gate can validate scoped macOS controlled-network Tauri evidence from `docs/qa/provider-live-smoke-macos.json` for `fox_controlled_fetch`, typed failure modes, controlled request observation, direct HTTP warning, and explicit external account/service scope limits. | Fetch real Fox resources for each command shape and record target client/session state, blocked/expired-session behavior, result count, and latency before claiming live Fox parity. | Controlled-network evidence is not real Fox service parity and does not prove account login state, anti-bot stability, service schema drift, or every legacy Fox command shape. |
| readboard sidecar probe through `readboard_sidecar_probe` | `external-validation-needed` | Domain tests and path checks prove structured ready/not-ready/runtime-unavailable/incompatible/timeout states. The repository smoke gate can also validate scoped macOS Tauri runtime evidence from `docs/qa/readboard-tauri-runtime-smoke-macos.json` for `runtime_started`, `sidecar_probe_ready`, and `sidecar_probe_unavailable`, plus scoped controlled image import MVP evidence from `docs/qa/readboard-image-import-smoke-macos.json` and scoped local-image backend decode evidence from `docs/qa/readboard-external-capture-mvp-macos.json`. | Real readboard parity still requires recorded environment details for sidecar version, launch method, port/path, timeout behavior, unavailable-sidecar handling, arbitrary OCR, target-client discovery, and real client parity. Cross-platform packaging evidence remains separate. | Scoped runtime evidence can prove the Tauri probe path and unavailable state semantics; controlled image import proves only path/base64 controlled fixtures; local-image backend decode proves only controlled fixture decoding. This is not arbitrary screenshot OCR proof, selected external capture proof, full legacy OCR parity, target-client discovery, or real client parity. |
| readboard protocol sync through `readboard_sidecar_sync_snapshot` | `external-validation-needed` | Domain tests prove protocol-line parsing and DTO normalization for board size, stones, move state, player-to-play, typed protocol errors, and timeout/cancel behavior. The repository smoke gate can validate scoped macOS Tauri runtime evidence for `protocol_line_sync` with snapshot id, board size, move number, stone count, and player-to-play; `target_state_change_sync` with distinct before/after snapshots and changed move or stone state; `arbitrary_ocr_not_covered`; and `external_capture_not_covered`. | Sync from a real target client/window state, verify board coordinates and move state, then repeat after sidecar restart or target-state change before claiming live readboard parity. | The scoped gate deliberately requires explicit not-covered fields for arbitrary OCR and external window/client capture; controlled image import has a separate scoped MVP gate, while arbitrary OCR and real external client/window capture remain pending unless separately validated. |

## Settings

| Legacy Capability | Current Status | Acceptance Evidence | External Gate | Notes |
| --- | --- | --- | --- | --- |
| Engine profile persistence in app data | `partial` | Tauri smoke creates, reloads, edits, and deletes profiles, then verifies persisted fields survive app restart and invalid paths produce clear errors. | Desktop runtime smoke on each supported OS. | Current persistence covers engine profiles, not every legacy preference. |
| Analysis cache storage and lookup | `partial` | Cache tests and smoke verify stable SGF cache keys, profile/engine filtering, save, lookup, delete, cache-hit UI state, and no stale analysis after SGF changes. | Cache migration evidence once storage schema stabilizes. | MVP SQLite cache exists. |
| Full Java/Swing configuration migration | `partial` | Repository smoke verifies a scoped migration safety surface: backend API wrappers expose `preview_legacy_config_migration`/`apply_legacy_config_migration` plus structured apply status, errors, written target labels, transactional/no-write flags, rollback state, rollback paths/errors, and raw written paths; App keeps failed applies non-applied and avoids reload; PreferencesPanel exposes path input, Preview, Apply, status, warnings, migrated fields, migration safety, written labels, rollback paths, and rollback errors. `legacy_config_corpus_migration_smoke` validates scoped corpus evidence at `docs/qa/legacy-config-corpus-migration-smoke.json` with at least eight fixtures covering minimal, full-engine, multi/conflict, UI review, Windows path, Unix path, Unicode/space, malformed partial, and unknown/deprecated classes, plus preview no-write, intended apply targets, preserved existing Next settings, invalid no-write, unsupported-key warnings, deterministic duplicate/conflict handling, rollback metadata, and false boundaries. | Real user config migration smoke, real rollback-failure exercise, exhaustive historical config corpus coverage, and documented mapping completeness before full migration parity is claimed. | This is scoped UI/API transactional/rollback surface plus repository corpus evidence only. It proves the migration entrypoint, safety metadata, and representative fixture classes are covered, not that every Java/Swing setting or historical config shape migrates. |

## Packaging

| Legacy Capability | Current Status | Acceptance Evidence | External Gate | Notes |
| --- | --- | --- | --- | --- |
| Release metadata and dry-run preflight | `complete` | `python3 scripts/validate_release_assets.py --verbose` and `python3 scripts/validate_release_workflow.py --verbose` pass; the release dry-run workflow remains read-only, unsigned, compile-only, artifact-contract checked, and covered for GitHub Actions Node action runtime hygiene. | None for dry-run metadata. | This validates release metadata and CI workflow hygiene, not production installers, signing/notarization, updater readiness, release publication, Windows/Linux installed-app proof, provider/readboard/OCR parity, or full legacy parity. |
| Production Tauri packaging for Windows, macOS, and Linux | `external-validation-needed` | Acceptance requires `npm run tauri:build` or equivalent release build per platform, artifact inspection, launch smoke, and recorded platform dependencies. The repository smoke gate validates recorded scoped packaging evidence from `docs/qa/multiplatform-packaging-smoke.json` for macOS, Windows, and Linux artifacts, signing-state records, dev-server absence, and SHA-256 checksums. It also validates recorded scoped installed macOS `.app` launch/window evidence from `docs/qa/installed-macos-app-smoke.json`, scoped installed app runtime workflow evidence from `docs/qa/installed-app-runtime-workflow-macos.json`, including backend command proof schema `lizzieyzy.installed-app-runtime-proof.v1`, Tauri runtime source, bundle/resource/appData metadata, asset validation, `profileStatus`, and `engineLaunchAttempt`, scoped bundled KataGo installed-app layout/launch-attempt evidence from `docs/qa/bundled-katago-installed-app-smoke-macos.json`, scoped installed app SGF workflow automation evidence from `docs/qa/installed-app-sgf-workflow-macos.json`, and scoped installed app live KataGo workflow evidence from `docs/qa/installed-app-katago-live-workflow-macos.json`. | Official signing, notarization, installer metadata, updater strategy, release publication, Windows/Linux installed-app smoke, native dialog/manual release smoke, and broader platform-specific installed-app smoke remain external release gates. | Scoped packaging smoke, installed macOS app launch/window evidence, scoped installed app runtime workflow evidence, scoped bundled KataGo installed-app layout/launch-attempt evidence, scoped packaged macOS app SGF workflow automation evidence, and scoped packaged macOS app live KataGo evidence are recorded; these gates are limited to scoped macOS runtime process/window/action/screenshot/backend-command proof with static-only/artifact-only rejection and unsigned/unnotarized/unpublished/updater-false boundaries. They are not formal signed/notarized release proof, updater proof, Windows/Linux installed-app proof, native dialog proof, WebView DOM proof, OCR/capture/provider/readboard parity, bundled engine launch success, full KataGo parity, full legacy parity, or production distribution readiness. |
| Bundled KataGo/runtime asset layout | `partial` | Repository smoke checks `runtime_asset_layout_surface`: backend wrappers call `resolve_runtime_asset_layout`/`validate_runtime_asset_layout`, and EngineSetupPanel displays bundled/runtime asset status while keeping local engine/model/config asset configuration available. The `bundled_katago_installed_app_smoke` gate validates `docs/qa/bundled-katago-installed-app-smoke-macos.json` as scoped packaged macOS `.app` runtime-backed evidence with app bundle hash/size, backend runtime proof, packaged runtime source, bundled asset layout validation, structured engine launch attempt, screenshot metadata, and false boundaries. | Actual installed-app bundled engine launch success, platform artifact inspection, signing/notarization, release inclusion, large-model bundling, and Windows/Linux installed-app proof remain required before claiming bundled runtime parity. | The current installed-app evidence records missing bundled KataGo resources as structured unavailable with `launchSucceeded=false`; it is not a successful bundled engine launch. Local asset configuration remains the supported path. This does not prove full bundled KataGo parity, full KataGo parity, release parity, signed/notarized release readiness, provider/readboard/OCR parity, or full legacy parity. |
