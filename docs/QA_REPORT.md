# QA Report

This report tracks the repository-local user-flow smoke skeleton for the current SGF annotation/readboard branch. It is intentionally scoped to checks that can run without external KataGo binaries, sidecars, provider accounts, or live network parity.

## Automated Smoke Gate

Run:

```sh
python3 scripts/smoke_user_flows.py --verbose
```

To collect the macOS local Tauri runtime UI evidence, run:

```sh
python3 scripts/smoke_tauri_runtime_ui.py --evidence-out docs/qa/tauri-runtime-ui-smoke-macos.json
```

This runtime script creates a temporary SGF, starts `npm --prefix apps/desktop run tauri:dev` with the `VITE_LIZZIEYZY_RUNTIME_SMOKE*`/`LIZZIEYZY_RUNTIME_SMOKE_REPORT_PATH` environment variables, waits for the app-produced report JSON, validates the required check names, and writes sanitized evidence when the report status is `pass`. CI does not run this GUI flow; CI only runs `smoke_user_flows.py` to validate the committed evidence schema and semantics.

To collect the scoped Desktop SGF Editing UX evidence after the macOS local Tauri runtime UI evidence is present, run:

```sh
python3 scripts/smoke_desktop_sgf_editing_ux.py --evidence-out docs/qa/desktop-sgf-editing-ux-smoke-macos.json
```

This evidence reuses the committed two-launch Tauri runtime UI smoke for tree navigation, comment/property/annotation editing, append/edit/reorder/delete, and save/readback/reopen semantics, then records source-surface/source-visible fields for the LegacyShell, toolbar/menu controls, SGF tree panel, annotation editor, selected-node state, dirty/saved status, and `nativeDialogClickCovered=false`. Its `collectionMethod` is `source_static_plus_tauri_runtime_chain`, with `runtimeDomObserved=false` and `screenshotObserved=false`. CI validates the committed JSON only; this scoped proof does not observe rendered DOM, take screenshots, click the OS-native dialog, cover OCR/external client/window capture, or prove full native desktop interactive UX parity.

The scoped browser-rendered DOM/click/screenshot evidence is recorded at:

```sh
docs/qa/desktop-ui-click-smoke-macos.json
```

The repository gate expects schema `lizzieyzy.desktop-ui-click-smoke.v1`, status `pass`, platform `macos`, `browserDomObserved=true`, `screenshotObserved=true`, `clickObserved=true`, at least two screenshot records with stable non-local paths and SHA-256 hashes, clicked controls, visible assertions, and boundaries with `nativeFileDialogCovered=false`. The same evidence now includes `legacyShellMenuActionSmoke` for browser-rendered LegacyShell menu actions: View/Candidates, View/Ownership, View/Policy, Engine/Profiles, Engine/Assets, Tools/Providers, Tools/Preferences, and Help/Backend status. `tauriWebviewDomObserved` must remain false unless the evidence includes explicit Tauri WebView proof. This is scoped browser-rendered click and menu-action proof only; it is not Tauri WebView DOM proof, OS-native menu proof, native file-dialog proof, full shortcut/layout parity, or full parity evidence.

The scoped macOS OS-native menu and keyboard shortcut evidence is recorded at:

```sh
docs/qa/native-menu-shortcut-smoke-macos.json
```

The repository gate expects schema `lizzieyzy.native-menu-shortcut-smoke.v1`, status `pass`, platform `macos`, `nativeMenuSurface=true`, `nativeMenuEventBridge=true`, `keyboardShortcutSurface=true`, `actionIdsAligned=true`, `inputEditingSafe=true`, and explicit false boundaries for full shortcut parity, full legacy menu parity, WebView DOM proof, full OS-native menu parity, release publication, production signing, notarization, provider/readboard/OCR coverage, and Windows/Linux coverage. This is scoped macOS native menu/shortcut surface evidence only; it is not full shortcut parity, full legacy menu parity, WebView DOM proof, formal release proof, provider/readboard/OCR proof, or Windows/Linux parity evidence.

The scoped Tauri desktop window/runtime screenshot evidence is recorded at:

```sh
docs/qa/tauri-window-runtime-smoke-macos.json
```

The repository gate expects schema `lizzieyzy.tauri-window-runtime-smoke.v1`, status `pass`, platform `macos`/`darwin`, `tauriRuntimeObserved=true`, `tauriWindowScreenshotObserved=true`, `browserFallbackUsed=false`, `webviewDomClickCovered=false`, `nativeDialogClickCovered=false`, at least one Tauri window screenshot with a stable non-local path and SHA-256 hash, source runtime evidence with schema `lizzieyzy.tauri-runtime-ui-smoke.v1` and status `pass`, and save/reopen semantic proof. This is scoped Tauri runtime/window screenshot proof only; it is not WebView DOM click proof, native dialog proof, installed packaged app/signing/release proof, OCR/capture proof, provider/readboard parity, or full legacy parity evidence.

The scoped installed macOS `.app` launch/window evidence is recorded at:

```sh
docs/qa/installed-macos-app-smoke.json
```

The repository gate expects schema `lizzieyzy.installed-macos-app-smoke.v1`, status `pass`, platform `macos`/`darwin`, app bundle existence metadata with size and SHA-256 hash, `launched=true`, `windowObserved=true`, `screenshotObserved=true`, `devServerAbsent=true`, `productionSigned=false`, `notarized=false`, `releasePublished=false`, at least one installed-app screenshot with a stable non-local path and SHA-256 hash, and exit/terminate success. This is scoped macOS packaged app launch/window proof only; it is not signed/notarized release proof, updater proof, Windows/Linux installed-app proof, native dialog proof, WebView DOM proof, OCR/capture proof, provider/readboard parity, or full legacy parity evidence.

The scoped native desktop SGF open/edit/save/reopen workflow evidence is recorded at:

```sh
docs/qa/native-desktop-sgf-workflow-macos.json
```

The repository gate expects schema `lizzieyzy.native-desktop-sgf-workflow.v1`, status `pass`, platform `macos`/`darwin`, app mode `tauri-dev` or `packaged-macos-app`, an explicit native workflow collection method, native open/save dialog coverage backed by concrete step records, operator/method metadata, sanitized SGF paths, screenshots with stable non-local paths, persisted edit evidence, and board/tree reopen invariants. The recorded evidence passes as scoped manual-assisted native desktop SGF workflow proof. For this scoped batch, `webviewDomAutomationCovered`, `fullAutomationCovered` for manual-assisted evidence, `fullLegacyParityCovered`, `productionSigned`, `notarized`, and `releasePublished` remain false. It does not claim full automation, WebView DOM automation, Windows/Linux installed-app coverage, OCR/capture/provider/readboard parity, signed/notarized release, release publication, or full legacy parity.

To collect the macOS live KataGo evidence, run:

```sh
python3 scripts/smoke_katago_live.py --engine /path/to/katago --model /path/to/model.bin.gz --config /path/to/analysis.cfg --evidence-out docs/qa/katago-live-smoke-macos.json
python3 scripts/smoke_tauri_katago_live.py --engine /path/to/katago --model /path/to/model.bin.gz --config /path/to/analysis.cfg --evidence-out docs/qa/katago-tauri-runtime-smoke-macos.json
```

The CLI runner invokes a real KataGo binary with a real model and config, probes `katago version`, runs one-position and batch `katago analysis` JSONL queries, validates response ids/root info/candidate output, captures stderr byte counts, and writes sanitized evidence. The Tauri runner launches `npm --prefix apps/desktop run tauri:dev` with `VITE_LIZZIEYZY_RUNTIME_SMOKE_PHASE=katago-live` and KataGo env vars, then waits for runtime evidence for startup, assets, analyze-once, analyze-game, and start/cancel. CI does not run KataGo or GUI; it only validates committed evidence.

To collect scoped macOS readboard runtime evidence, run the Tauri readboard smoke runner and record:

```sh
docs/qa/readboard-tauri-runtime-smoke-macos.json
```

The repository gate expects schema `lizzieyzy.readboard-tauri-runtime-smoke.v1`, status `pass`, platform `macos`, and passing semantic checks for Tauri runtime startup, sidecar probe ready/unavailable states, protocol-line sync with `snapshotId`, board size, move number, stone count, and player-to-play, target-state-change sync with distinct before/after snapshots and stable board size, explicit `arbitrary_ocr_not_covered` and `external_capture_not_covered` boundaries, and a pointer that controlled image import is covered by `docs/qa/readboard-image-import-smoke-macos.json`. CI validates the committed JSON only; it does not run the GUI, sidecar, arbitrary OCR, or any external client/window capture.

To collect scoped macOS provider runtime evidence, run the Tauri provider smoke runner and record:

```sh
docs/qa/provider-live-smoke-macos.json
```

The repository gate expects schema `lizzieyzy.provider-live-smoke.v1`, status `pass`, platform `macos`, and passing semantic checks for Tauri runtime startup, controlled-network Yike fetch, controlled-network Fox fetch, typed provider failure modes, controlled HTTP request observation, explicit non-offline-parser-only evidence, and explicit external account scope limits. CI validates the committed JSON only; this scoped evidence does not cover real Fox/Yike services, account login state, anti-bot stability, or service schema drift.

To collect scoped multiplatform packaging smoke evidence, run the packaging smoke runner and record:

```sh
docs/qa/multiplatform-packaging-smoke.json
```

The repository gate expects schema `lizzieyzy.multiplatform-packaging-smoke.v1`, status `pass`, macOS/Windows/Linux artifact checks, signing-state records, dev-server-absence checks, and SHA-256 checksums. CI validates the committed JSON only; this scoped evidence records packaging smoke state and does not claim official signing, notarized release distribution, updater readiness, or full legacy parity.

Covered checks:

| Area | Status | Evidence |
| --- | --- | --- |
| Golden SGF fixtures | Automated | Verifies `tests/golden/basic_19x19.sgf`, `tests/golden/sgf_compat_variations.sgf`, `tests/golden/sgf_ff4_compat.sgf`, and `tests/golden/sgf_reorder_variations.sgf` exist and are non-empty. |
| SGF compatibility fixture shape | Automated | Verifies the FF4 compatibility fixture contains variations, comments, setup properties, and labels. |
| SGF variation reorder fixture shape | Automated | Verifies the reorder fixture has three sibling variations under one parent, comments, an unknown property, labels/annotations, and a nested subtree. |
| Package entry points | Automated | Verifies root and desktop package scripts expose dev/build/Tauri entry points. |
| SGF edit Tauri commands | Automated | Verifies `update_sgf_node_comment`, `append_sgf_move`, `delete_sgf_node`, `update_sgf_node_properties`, and `reorder_sgf_variation` are defined as Tauri commands and registered in `generate_handler!`. |
| SGF node property editing command | Automated | Verifies the local command surface for updating SGF node properties is present. This is command/fixture evidence only, not a reopened-file desktop proof. |
| SGF variation reorder command | Automated | Verifies the local command surface for sibling variation reorder is present and the reorder fixture can represent the target tree shape. This is not interactive UI smoke evidence. |
| LegacyShell main menu surface and actions | Automated + scoped evidence gate recorded | Statically verifies the `View`, `Engine`, `Tools`, and `Help` menu entries in `LegacyShell.tsx` are present, identifiable by label or `data-testid`, and are not disabled-only placeholders. `legacy_shell_menu_action_smoke` now validates committed browser-rendered evidence that View/Candidates, View/Ownership, View/Policy, Engine/Profiles, Engine/Assets, Tools/Providers, Tools/Preferences, and Help/Backend status clicks update LegacyShell menu action attributes and focus visible target surfaces. This is scoped browser-rendered menu-action evidence only; it is not Tauri WebView DOM proof, OS-native menu proof, native dialog proof, full shortcut/layout parity, or full LegacyShell parity. |
| macOS OS-native menu and keyboard shortcut surface | Scoped evidence gate recorded | `smoke_user_flows.py` marks `native_menu_shortcut_smoke` PASS only when `docs/qa/native-menu-shortcut-smoke-macos.json` has schema `lizzieyzy.native-menu-shortcut-smoke.v1`, status `pass`, platform `macos`, native menu surface, native-menu event bridge, keyboard shortcut surface, aligned action ids, input-editing safety, and strict false boundaries for full shortcut parity, full legacy menu parity, WebView DOM proof, full OS-native menu parity, release/signing/notarization, provider/readboard/OCR, and Windows/Linux claims. This is scoped macOS native menu/shortcut surface evidence only. |
| Native SGF save/read-back refresh surface | Automated | Statically verifies native SGF save writes through `write_sgf_file`, reads the saved file back through `read_sgf_file`/`readSgfDocument`, and refreshes App state by parsing, replaying, rebuilding the tree, and checking cache from the read-back SGF text. This is repository-local evidence only; it is not real desktop GUI save/reopen smoke. |
| SGF existing move edit surface | Automated | Statically verifies `sgf_existing_move_edit_surface`: `edit_sgf_move` is defined/registered and the repository-local backend/App/SgfTreePanel edit-existing-move surface is wired. This is repository-local evidence only; it is not interactive desktop edit/save/reopen proof. |
| SGF annotation surface | Automated | Statically verifies `sgf_annotation_surface`: SgfAnnotationPanel exposes TR/SQ/CR/MA/SL/LB/AR/LN add/update/remove controls, App routes saves through `updateSgfNodeProperties`, save failures surface as annotation errors, and runtime smoke includes `annotation_edit` add/update/remove semantics. This is scoped SGF annotation persistence evidence only. |
| Legacy import/capture helper surface | Automated | Statically verifies `legacy_import_capture_helper_surface`: provider domain/API wrappers define a scoped legacy helper contract, ProviderPanel exposes SGF/payload and protocol snapshot helper paths, and OCR/image plus external window/client capture helpers show structured recoverable-unsupported state with `imported=false`, no SGF import, and no board replacement. This is scoped UI/API boundary evidence only; it is not real OCR, real external client/window capture, or full legacy helper parity. |
| Legacy Java/Swing config migration safety and corpus | Automated + scoped evidence recorded | Statically verifies `legacy_config_migration_surface`: backend API wrappers expose structured apply DTO fields for status, error message, written target labels, transactional/no-write flags, rollback state, rollback paths/errors, and raw written paths; App treats failed applies as non-applied and avoids reload; PreferencesPanel exposes path input, Preview/Apply actions, status, warnings, migrated fields, and migration safety details. `legacy_config_corpus_migration_smoke` also validates `docs/qa/legacy-config-corpus-migration-smoke.json` for a scoped repository corpus with at least eight fixtures covering minimal, full-engine, multi/conflict, UI review, Windows path, Unix path, Unicode/space, malformed partial, and unknown/deprecated cases plus preview no-write, intended apply targets, preserved existing Next settings, invalid no-write, unsupported-key warnings, deterministic duplicate/conflict handling, rollback metadata, and explicit false boundaries. This is scoped UI/API plus repository corpus evidence only; it is not real-user migration proof, real rollback-failure exercise, full historical config parity, or full Java/Swing config parity. |
| macOS Tauri runtime UI smoke evidence | Scoped evidence gate | `smoke_user_flows.py` marks `ui_tauri_runtime_smoke` PASS only when `docs/qa/tauri-runtime-ui-smoke-macos.json` has schema `lizzieyzy.tauri-runtime-ui-smoke.v1`, status `pass`, platform `macos`, all required checks passing, semantic evidence for annotation add/update/remove, existing-move edit, variation reorder target index, delete absence, save/read-back, board-state invariants, and two-launch save/reopen proof fields: `secondLaunch`, `reopen`, and `afterReopen`. |
| Desktop SGF editing UX smoke evidence | Scoped evidence gate recorded | `smoke_user_flows.py` marks `desktop_sgf_editing_ux_smoke` PASS only when `docs/qa/desktop-sgf-editing-ux-smoke-macos.json` has schema `lizzieyzy.desktop-sgf-editing-ux-smoke.v1`, status `pass`, platform `macos`, `collectionMethod=source_static_plus_tauri_runtime_chain`, `runtimeDomObserved=false`, `screenshotObserved=false`, all required source/static UI surface checks passing, source-visible fields for LegacyShell, toolbar/menu controls, tree panel, annotation editor, selected-node state, dirty/saved status, `nativeDialogClickCovered=false`, and runtime-chain coverage for tree navigation, comment/property/annotation, append/edit/reorder/delete, and save/readback/reopen. This is source-surface plus runtime-chain evidence only, not runtime-rendered DOM/screenshot/click proof. |
| Desktop browser-rendered UI click smoke | Scoped evidence gate recorded | `smoke_user_flows.py` marks `desktop_ui_click_smoke` PASS only when `docs/qa/desktop-ui-click-smoke-macos.json` has schema `lizzieyzy.desktop-ui-click-smoke.v1`, status `pass`, platform `macos`, browser DOM/click/screenshot observation flags, multiple SHA-256 screenshot records with stable non-local paths and SHA-256 hashes, clicked controls, visible assertions, and boundaries that keep native file dialog coverage false and Tauri WebView DOM coverage false unless explicit proof is recorded. `legacy_shell_menu_action_smoke` marks PASS only when the same evidence includes structured LegacyShell menu clickedControls, activeTargets, visibleAssertions, and boundaries for the scoped browser-rendered menu actions. This is scoped browser-rendered click/menu-action evidence only. |
| Tauri window/runtime screenshot smoke | Scoped evidence gate recorded | `smoke_user_flows.py` marks `tauri_window_runtime_smoke` PASS only when `docs/qa/tauri-window-runtime-smoke-macos.json` has schema `lizzieyzy.tauri-window-runtime-smoke.v1`, status `pass`, platform `macos`/`darwin`, Tauri runtime and Tauri window screenshot observation flags, no browser fallback, no WebView DOM click claim, no native dialog click claim, stable non-local screenshot path with SHA-256, source runtime evidence schema/status, and save/reopen semantic proof. This is scoped Tauri runtime/window screenshot evidence only. |
| Legacy layout parity smoke | Scoped evidence recorded | `smoke_user_flows.py` validates `docs/qa/legacy-layout-parity-smoke-macos.json` as scoped layout evidence only. The gate requires schema `lizzieyzy.legacy-layout-parity-smoke.v1`, status `pass`, platform `macos`/`darwin`, stable SHA-256 screenshot records for default review, SGF editing, KataGo analysis, provider/readboard, and engine/preferences, at least three viewports including `1280x840`, narrow desktop, and short window, visible assertions for board, toolbar/menu, SGF tree, annotation/comment/properties, analysis panel, winrate, candidates/PV, cache/status, provider/readboard, and engine/preferences, no critical overlap/clipping, and false boundaries for pixel-perfect parity, full legacy UI parity, full shortcut parity, release parity, OCR capture parity, and full legacy parity. |
| Installed macOS `.app` launch smoke | Scoped evidence gate recorded | `smoke_user_flows.py` marks `installed_macos_app_smoke` PASS only when `docs/qa/installed-macos-app-smoke.json` has schema `lizzieyzy.installed-macos-app-smoke.v1`, status `pass`, platform `macos`/`darwin`, app bundle exists metadata with size/hash, installed app launch and window observation, screenshot observation with stable non-local path and SHA-256, dev-server absence, explicit unsigned/unnotarized/unpublished boundaries, and exit/terminate success. This is scoped macOS packaged app launch/window evidence only. |
| Native desktop SGF open/edit/save/reopen workflow | Scoped evidence gate recorded | `smoke_user_flows.py` marks `native_desktop_sgf_workflow` PASS only when `docs/qa/native-desktop-sgf-workflow-macos.json` has schema `lizzieyzy.native-desktop-sgf-workflow.v1`, status `pass`, platform `macos`/`darwin`, scoped app mode, explicit collection method, required native workflow checks passing, native open/save dialog records with operator/method/SGF path/screenshot metadata, stable screenshot SHA-256 records, persisted edit evidence, board/tree reopen invariants, sanitized paths, and explicit boundaries excluding WebView DOM automation, full automation for manual-assisted evidence, full legacy parity, release publication, signing, notarization, Windows/Linux, OCR/capture, provider, and readboard claims. This is scoped manual-assisted native desktop workflow proof only. |
| macOS live KataGo smoke evidence | Scoped evidence gate recorded | `smoke_user_flows.py` marks `katago_live_smoke` PASS only when both evidence files pass: `docs/qa/katago-live-smoke-macos.json` for real CLI `katago analysis`, and `docs/qa/katago-tauri-runtime-smoke-macos.json` for the Tauri runtime path with startup, assets, analyze-once, analyze-game, failure-mode, and confirmed start/cancel checks. |
| KataGo review workflow UX resilience | Scoped evidence gate recorded | `smoke_user_flows.py` marks `katago_review_workflow_ux_smoke` PASS only when `docs/qa/katago-review-workflow-ux-smoke-macos.json` has schema `lizzieyzy.katago-review-workflow-ux-smoke.v1`, status `pass`, platform `macos`, `collectionMethod=source_static_plus_stubbed_ui_flow`, source facts for visible progress/current/total/job id/session, cancel/restart, cache-hit restore, engine failure messaging, and stale-analysis guard, plus true scoped proof fields for progress/cancel/restart/cache/failure/stale/source validation. It records `liveKataGoObserved=false` and false boundaries for full legacy analysis parity, release/signing/notarization, provider/readboard/OCR, and Windows/Linux claims. This is scoped source-static plus stubbed UI-flow resilience evidence only, not live KataGo behavior or full legacy analysis parity. |
| KataGo live desktop workflow | Scoped evidence recorded | `smoke_user_flows.py` validates `docs/qa/katago-live-desktop-workflow-smoke-macos.json` as scoped live desktop workflow proof only. The gate requires schema `lizzieyzy.katago-live-desktop-workflow-smoke.v1`, status `pass`, platform `macos`, live KataGo observation through explicit or proof-backed runtime/assets evidence, `browserFallbackUsed=false`, required checks for runtime, assets, progress, cancel/restart, completion, cache save, cache-hit restore, stale-cache prevention, engine failure, browser fallback exclusion, and scope boundaries. Cache-hit restore must include frame, candidate, and winrate evidence. Boundaries must keep `fullLegacyAnalysisParity=false`, `providerReadboardParity=false`, `releaseParity=false`, and `arbitraryOcrParity=false`. This is not full legacy analysis parity, provider/readboard parity, release parity, or arbitrary OCR parity. |
| Bundled/runtime asset layout surface | Scoped repository surface recorded | `smoke_user_flows.py` marks `runtime_asset_layout_surface` PASS when frontend backend wrappers call `resolve_runtime_asset_layout`/`validate_runtime_asset_layout` and EngineSetupPanel shows bundled/runtime asset status while preserving local engine/model/config asset fields. Large KataGo models are not bundled, and this is not installed-app bundled-engine launch proof. |
| macOS scoped readboard runtime evidence | Scoped evidence gate recorded | `smoke_user_flows.py` marks `readboard_live_smoke` PASS only when `docs/qa/readboard-tauri-runtime-smoke-macos.json` validates the scoped Tauri runtime probe/protocol evidence, including snapshot/change semantics, and explicitly records that OCR and external client/window capture are not covered. |
| Readboard controlled image import MVP | Scoped evidence gate recorded | `smoke_user_flows.py` marks `readboard_image_import_smoke` PASS only when `docs/qa/readboard-image-import-smoke-macos.json` has schema `lizzieyzy.readboard-image-import-smoke.v1`, status `pass`, platform `macos`, path and base64 controlled image import evidence, invalid-image and non-board-image rejection, snapshot/board-size/stone-count/to-play verification, protocol regression evidence, and `fullOcrParity=false` plus `externalCaptureCovered=false`. This is scoped controlled image import MVP evidence only; external window/client capture, arbitrary screenshot OCR, and full legacy OCR parity remain pending. |
| Readboard controlled image OCR corpus | Scoped evidence gate recorded | `smoke_user_flows.py` marks `readboard_image_ocr_corpus_smoke` PASS only when `docs/qa/readboard-image-ocr-corpus-smoke-macos.json` has schema `lizzieyzy.readboard-image-ocr-corpus-smoke.v1`, status `pass`, platform `macos`, a repo-relative fixture manifest with verified SHA-256/size metadata, path/base64 equivalence, invalid/non-board/truncated rejection, board-size and stone-count coverage, hash invariants, external capture unsupported-contract evidence, and false boundaries for full OCR parity, external window capture, real client capture, and full readboard parity. This is scoped controlled image corpus evidence only; arbitrary OCR and external capture remain pending. |
| macOS scoped provider controlled-network evidence | Scoped evidence gate recorded | `smoke_user_flows.py` marks `provider_live_smoke` PASS only when `docs/qa/provider-live-smoke-macos.json` validates controlled-network Yike/Fox runtime fetches, typed failure modes, and explicit external account/service scope exclusions. |
| Multiplatform packaging smoke evidence | Scoped evidence gate recorded | `smoke_user_flows.py` marks `multiplatform_packaging_smoke` PASS only when `docs/qa/multiplatform-packaging-smoke.json` validates scoped macOS/Windows/Linux artifact, signing-state, dev-server-absence, and checksum evidence. This is not official signing, notarization, release publication, updater readiness, or full legacy parity proof. |
| CI Node action runtime hygiene | Scoped validation recorded | `validate_release_workflow.py` covers GitHub Actions Node action runtime declarations as release workflow hygiene. This is scoped CI metadata validation only; it is not signing, notarization, updater readiness, official release publication, Windows/Linux installed-app proof, provider/readboard/OCR parity, or full parity proof. |

Current repository-local alpha gate result:

```text
python3 scripts/smoke_user_flows.py --verbose
User-flow smoke: 44 passed, 0 failed, 0 pending.
```

With the native SGF save/read-back, existing-move-edit, scoped annotation editor surface, scoped legacy import/capture helper surface, scoped Desktop SGF Editing UX evidence, scoped browser-rendered DOM/click/screenshot evidence, scoped browser-rendered LegacyShell menu-action evidence, scoped macOS native menu/shortcut evidence target, scoped Tauri runtime/window screenshot evidence, scoped Tauri WebView DOM/click evidence, scoped legacy layout evidence, scoped installed macOS `.app` launch/window evidence, scoped native desktop SGF open/edit/save/reopen workflow evidence, legacy config migration transactional/rollback safety surface plus scoped corpus migration evidence, bundled/runtime asset layout surface, macOS local Tauri runtime UI smoke, macOS live KataGo smoke, scoped KataGo review workflow UX resilience evidence, scoped live KataGo desktop workflow evidence, scoped macOS readboard runtime evidence, scoped readboard controlled image import MVP evidence, scoped controlled readboard image OCR corpus evidence, scoped macOS provider controlled-network evidence, scoped multiplatform packaging smoke evidence, and scoped CI Node action runtime hygiene validation included, the existing scoped gates are complete for their current evidence. This is still not a 100% or release-ready gate because full historical config parity, real-user config migration smoke, real rollback-failure exercise, signed/notarized release proof, updater readiness, Windows/Linux installed-app proof, full legacy UI/layout parity, full legacy analysis parity, arbitrary screenshot OCR, external window/client capture, real client capture, full readboard parity, full legacy OCR parity, full legacy helper migration, real Fox/Yike service/account parity, official release publication, full shortcut/layout parity, full OS-native menu parity, and broader legacy parity remain outside the scoped proof.

## Deferred Runtime Gates

These are the scoped/runtime gates and the remaining evidence boundaries:

| Gate | Status | Required evidence before completion |
| --- | --- | --- |
| macOS local runtime save/reopen proof | Scoped evidence recorded | `docs/qa/tauri-runtime-ui-smoke-macos.json` records the two-launch macOS runtime smoke with `annotation_edit` and `afterReopen.annotationsVerified` alongside `firstLaunch`, `secondLaunch`, `saveReopenProof`, `reopen`, comments, properties, move count, and board-state verification. This is not native file dialog/manual release proof. |
| Desktop SGF editing UX proof | Scoped evidence recorded | `docs/qa/desktop-sgf-editing-ux-smoke-macos.json` records source-surface/source-visible fields for LegacyShell, toolbar/menu, SGF tree, annotation editor, selected-node UX state, dirty/saved status, and runtime-chain coverage for tree navigation, comment/property/annotation, append/edit/reorder/delete, and save/readback/reopen. It explicitly records `collectionMethod=source_static_plus_tauri_runtime_chain`, `runtimeDomObserved=false`, `screenshotObserved=false`, and `nativeDialogClickCovered=false`, so runtime-rendered DOM/screenshot/click proof and native dialog/manual release proof remain pending. |
| Desktop browser-rendered click and LegacyShell menu-action proof | Scoped evidence recorded | `docs/qa/desktop-ui-click-smoke-macos.json` records scoped browser DOM observation, screenshots with stable non-local paths and SHA-256 hashes, clicked controls, visible assertions, and `legacyShellMenuActionSmoke` coverage for View/Candidates, View/Ownership, View/Policy, Engine/Profiles, Engine/Assets, Tools/Providers, Tools/Preferences, and Help/Backend status target focus. Boundaries keep `nativeFileDialogCovered=false`, `tauriWebviewDomObserved=false`, OS-native menu proof false, and full shortcut/layout parity false. This is not native file dialog, Tauri WebView, OS-native menu, full shortcut/layout parity, or full parity proof. |
| macOS OS-native menu and shortcut surface proof | Scoped evidence recorded | `docs/qa/native-menu-shortcut-smoke-macos.json` records scoped native menu surface, native-menu event bridge, keyboard shortcut surface, action-id alignment, and input-editing safety. The gate now cross-checks Rust/frontend event names, `native_menu_contract` registration, canonical action ids, and exact File/Game/Analysis/View/Engine/Tools/Help groups.  Boundaries keep full shortcut parity, full legacy menu parity, WebView DOM proof, full OS-native menu parity, release/signing/notarization, provider/readboard/OCR, and Windows/Linux parity false. |
| Tauri runtime/window screenshot proof | Scoped evidence recorded | `docs/qa/tauri-window-runtime-smoke-macos.json` records scoped Tauri runtime/window screenshot observation with no browser fallback, no WebView DOM click coverage, no native dialog click coverage, stable non-local screenshot paths, valid source runtime evidence, and save/reopen semantic proof. This is not WebView DOM click proof, native dialog proof, installed packaged app/signing/release proof, OCR/capture proof, provider/readboard parity, or full legacy parity. |
| Tauri WebView DOM/click proof | Scoped evidence recorded | `docs/qa/tauri-webview-dom-click-smoke-macos.json` records scoped macOS Tauri WebView runtime, DOM, click, and visible-target evidence with no browser fallback. The gate requires at least four clicked controls, at least four visible target assertions, required runtime/check records, and false boundaries for full layout parity, full shortcut parity, full legacy parity, release parity, and OCR capture parity. This is not native dialog proof, full layout/shortcut/release/OCR parity, or full legacy parity. |
| Legacy layout evidence | Scoped evidence recorded | `docs/qa/legacy-layout-parity-smoke-macos.json` records scoped layout evidence for default review, SGF editing, KataGo analysis, provider/readboard, and engine/preferences screenshots across required desktop viewports. It does not prove pixel-perfect parity, full legacy UI parity, full shortcut parity, release parity, OCR capture parity, or full legacy parity. |
| Installed macOS packaged app launch/window proof | Scoped evidence recorded | `docs/qa/installed-macos-app-smoke.json` records scoped installed macOS `.app` bundle metadata, launch success, window observation, screenshot observation, dev-server absence, stable non-local screenshot paths, app size/hash, explicit unsigned/unnotarized/unpublished boundaries, and exit/terminate success. This is not signed/notarized release proof, updater proof, Windows/Linux installed-app proof, native dialog proof, WebView DOM proof, OCR/capture proof, provider/readboard parity, or full legacy parity. |
| Native desktop SGF workflow proof | Scoped evidence recorded | `docs/qa/native-desktop-sgf-workflow-macos.json` records scoped manual-assisted native desktop SGF open/edit/save/reopen workflow evidence: native open/save dialog step records, operator/method metadata, sanitized SGF/screenshot/app/log paths, screenshot hashes, persisted edit evidence, board/tree reopen invariants, and boundaries that keep WebView DOM automation, full automation for manual-assisted evidence, full legacy parity, signing, notarization, release publication, Windows/Linux installed-app, OCR/capture, provider, and readboard claims false. |
| Broader real Tauri UI flow | Pending | Record any additional manual desktop runtime coverage beyond the macOS local smoke, including native file dialog open/save paths and exploratory UI behavior. |
| KataGo analysis flow | Scoped macOS evidence recorded | `docs/qa/katago-live-smoke-macos.json` and `docs/qa/katago-tauri-runtime-smoke-macos.json` were produced with a real KataGo binary, model, and config. The CLI evidence covers version probe plus one-position and batch `katago analysis`; the Tauri runtime evidence covers real desktop backend startup, assets, analyze-once, analyze-game, missing-asset failure mode, and confirmed start/cancel. `docs/qa/katago-review-workflow-ux-smoke-macos.json` separately records scoped source-static plus stubbed UI-flow resilience evidence for progress, cancel/restart, cache restore, engine failure messaging, and stale-analysis prevention with `liveKataGoObserved=false`. `docs/qa/katago-live-desktop-workflow-smoke-macos.json` records scoped live desktop workflow evidence for live progress, cancel/restart, completion, cache save/hit restore, stale-cache prevention, and engine failure proof. Full legacy analysis parity, provider/readboard parity, release parity, and arbitrary OCR parity remain outside these scoped gates. |
| Bundled/runtime asset layout | Scoped repository surface recorded | EngineSetupPanel displays bundled/runtime asset layout status from the Tauri runtime layout commands and keeps local engine/model/config asset configuration visible. This scoped gate does not bundle large KataGo models, does not prove installed-app bundled-engine launch, and does not prove signing, notarization, or release asset inclusion. |
| Readboard sidecar flow | Scoped macOS evidence recorded | `docs/qa/readboard-tauri-runtime-smoke-macos.json` records scoped macOS Tauri runtime evidence for sidecar probe ready/unavailable states, protocol-line sync with snapshot/move/stone semantics, target-state-change sync with distinct before/after snapshots and stable board size, plus honest boundaries named `arbitrary_ocr_not_covered` and `external_capture_not_covered`. `docs/qa/readboard-image-import-smoke-macos.json` separately records controlled image path/base64 import MVP evidence with invalid/non-board rejection and snapshot verification while keeping `fullOcrParity=false` and `externalCaptureCovered=false`. `docs/qa/readboard-image-ocr-corpus-smoke-macos.json` records scoped controlled image OCR corpus evidence for fixture manifest metadata, path/base64 equivalence, invalid/non-board/truncated rejection, board-size/stone-count coverage, hash invariants, and unsupported external capture contract while keeping full OCR/readboard/external capture claims false. This gate is deliberately scoped: it does not prove arbitrary screenshot OCR, real external client/window capture, full legacy OCR parity, full readboard parity, or cross-platform packaging. |
| Legacy import/capture helper surface | Scoped repository surface recorded | ProviderPanel records visible SGF/payload and protocol snapshot helper paths, plus structured recoverable-unsupported OCR/image and external window/client capture helpers. Unsupported helper states explicitly report no SGF import and no board replacement with guessed, stale, or partial data. This is not real OCR, real external client/window capture, or full legacy helper migration. |
| Fox/Yike provider flow | Scoped controlled-network evidence recorded | `docs/qa/provider-live-smoke-macos.json` records scoped macOS Tauri runtime evidence for controlled-network Yike/Fox fetches, request observation, and typed provider failure modes. Real Fox/Yike service coverage, account/session login state, anti-bot stability, rate-limit/session-expiry behavior, and service schema drift remain pending and must not be inferred from controlled-network evidence. |
| Platform packaging smoke | Scoped evidence recorded | `docs/qa/multiplatform-packaging-smoke.json` records scoped macOS/Windows/Linux packaging smoke evidence for artifacts, signing-state recording, dev-server absence, and SHA-256 checksums. This gate does not prove official signing, notarization, release publication, updater readiness, store distribution, or full release parity. |
| CI/release workflow hygiene | Scoped validation recorded | The release workflow validator covers GitHub Actions Node action runtime declarations. This is CI metadata hygiene only, not production release readiness. |
| Legacy Java/Swing config migration | Scoped repository safety surface and corpus evidence recorded | PreferencesPanel now exposes a migration entrypoint with path input, Preview/Apply actions, status, warnings, migrated-field output, and structured migration safety state for transactional/no-write/rollback results. App keeps failed applies non-applied and avoids preference/profile reload on failure. `docs/qa/legacy-config-corpus-migration-smoke.json` records scoped corpus coverage for the required fixture classes and no-write/apply/preserve/warn/conflict/rollback metadata checks. Real-user migration smoke, real rollback-failure exercise, full historical config parity, and full Java/Swing config parity remain outside this scoped proof. |

## Current QA Position

Passing `scripts/smoke_user_flows.py` means the repository still has the local fixture and command surface needed for SGF comment editing, node property editing, append move/pass, delete selected non-root node/subtree, and variation reorder foundations. It does not prove that a user can complete those flows in the native desktop UI, save them, reopen the file, and get identical board/tree state.

As of this gate, `scripts/smoke_user_flows.py --verbose` reports `44 passed, 0 failed, 0 pending`. Existing scoped gates pass, including `readboard_image_ocr_corpus_smoke` for controlled fixture-manifest OCR corpus evidence and unsupported external capture boundaries; `legacy_config_corpus_migration_smoke` for scoped repository fixture-class corpus coverage, no-write/apply/preserve/warn/conflict/rollback metadata checks, and explicit false boundaries; `legacy_layout_parity_smoke` for scoped layout screenshots/viewports/visible-target/overlap evidence; and `katago_live_desktop_workflow_smoke` for scoped live progress, cancel/restart, completion, cache save/hit restore, stale-cache prevention, engine failure, and explicit false boundaries. Real-user config migration smoke, real rollback-failure exercise, full historical config parity, full Tauri WebView layout parity, signed/notarized release proof, updater readiness, Windows/Linux installed-app proof, bundled installed-app engine launch, large-model bundling, release asset inclusion, full legacy UI/layout parity, full legacy analysis parity, provider/readboard parity, arbitrary screenshot OCR, external window/client capture, real client capture, full readboard parity, full legacy OCR parity, real Fox/Yike service/account coverage, anti-bot stability, service schema drift, full OS-native menu parity, full shortcut/layout parity, official release publication, and full production distribution remain outside the scoped proof. Do not claim bundled KataGo runtime parity, Fox/Yike full parity, full readboard parity, formal production release, full automation, Tauri WebView DOM parity, full native-dialog desktop release parity, full LegacyShell UI parity, full Java/Swing config migration parity, full capture/import/OCR parity, arbitrary screenshot OCR, external window/client capture, full legacy OCR parity, full legacy analysis parity, full layout/UI parity, or full legacy parity until the corresponding desktop/runtime/release checks above are recorded with environment details.
